
# Preparation for Python examples
- Install Python, Docker, Ollama, and the IDE (PyCharm, VS Code, ...)
- Optional: Install Maven for CLI
- Create a virtual environment: python3 -m venv venv
- Activate the environment: source venv/bin/activate
- Note: Under Windows the commands differ, e.g., venv\Scripts\activate.bat
- Execute the examples:
  - Read the README.md in the examples folder
  - 